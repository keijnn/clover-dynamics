//import modules
import { createRoute } from "atomic-router"

const route = createRoute()

const Page = () => {
    return (
        <>Home</>
    )
}

export const HomePage = {
    route,
    Page
}